# Module blacklist for ShedOS.
#
# pcspkr is the legacy "PC speaker" driver. On systems that still have a
# physical motherboard speaker, it produces a piercing beep on terminal
# bell and on certain kernel events. Modern hardware almost always lacks
# the physical buzzer entirely, so this is just dead code with a chance
# of unwanted noise.

blacklist pcspkr
