[Unit]
Description=Detect drift between /etc/shedos/system.toml and live state, refresh the waybar indicator, and notify when drift appears

[Service]
Type=oneshot
Environment=HOME=%h
ExecStart=/usr/bin/shedman doctor --tick
