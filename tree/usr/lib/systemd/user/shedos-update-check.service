[Unit]
Description=Poll for ShedOS / Arch updates, refresh the waybar indicator, and notify on new updates
Documentation=man:checkupdates(8)

[Service]
Type=oneshot
Environment=HOME=%h
ExecStart=/usr/bin/shedman updates --tick
