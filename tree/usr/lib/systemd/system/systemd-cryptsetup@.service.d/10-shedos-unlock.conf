# Every container waits for the one prompt. Pulling the unlocker in from here
# rather than from a target is what makes it reliable: the generator writes a
# complete unit per container and pulls it in through the device unit, so
# cryptsetup.target is not guaranteed to be started at all in the initrd.
[Unit]
Wants=shedos-unlock.service
After=shedos-unlock.service
