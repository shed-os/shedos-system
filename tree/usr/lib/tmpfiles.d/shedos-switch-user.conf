# Preselect handoff for lock-screen fast user switching. The switch-user
# helper (root) writes the chosen username here; the greeter reads it on
# startup and clears it. Greeter-owned so that clear succeeds.
d /run/shedos 0755 greeter greeter -
